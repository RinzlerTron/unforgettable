{
  "_comment": "CockroachDB Cloud managed MCP config. Copy into your MCP client config (Claude Code: .mcp.json; Cursor: mcp.json), set <your-cluster-id> from the Cloud Console's Connect > MCP dialog, then authenticate via the OAuth flow (pick read-only). Try: 'show the facts table in the unforgettable database'.",
  "mcpServers": {
    "cockroachdb-cloud": {
      "type": "http",
      "url": "https://cockroachlabs.cloud/mcp",
      "headers": {
        "mcp-cluster-id": "<your-cluster-id>"
      }
    }
  }
}
